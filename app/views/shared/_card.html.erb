  <div class="card-group">
    <div class="col-xl-12">
      <h5>Some of our popular routes
        <small><em><%= link_to "(see all)", journeys_path %></em></small>
      </h5>
    </div>

    <!-- LIST ALL PUBLIC ROUTES - PRIVACY FALSE -->

    <% @journeys.where(privacy: false).limit(10).each do |listing| %>

    <div class="col-lg-2">
      <div class="card">
        <img class="card-img-top" src="<%=listing.route_url%>">
        <div class="card-body">
          <h6 class="card-title">
            <%= link_to "#{listing.name}", "/journeys/#{listing.id}}" %>
          </h6>
          <p>
            From: <%= listing.origin %> </br>
            To: <%= listing.destination %>
          </p>
          <div class="card-avatar">
            <% user = User.find(listing.user_id)%>
            <%= render "shared/user-avatar", user: user %>
          </div>
        </div>
        <div class="card-footer">
          <small>
            <%= link_to "see route", "/journeys/#{listing.id}}" %>
          </small>
        </div>
      </div>
    </div>

    <% end %>
  </div>
